class Solution {
public:
    bool doesAliceWin(string s) {
        int cnt=0;
        for(auto c:s){
            if(c=='a' || c=='e' || c=='i' || c=='o' ||c=='u'){
                cnt++;
            }
        }
        if(cnt==0)
        return false;
        else
        return true;
    }
};